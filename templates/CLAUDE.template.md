<!-- ════════════════════════════════════════════════════════════════
  사용법 (다 읽고 이 안내 블록은 지우세요)
  1) 이 파일을 새 프로젝트 루트에 `CLAUDE.md` 로 복사.
  2) 아래 <...> 자리를 그 프로젝트에 맞게 채움. 모르는 칸은 비워두지 말고 "아직 없음" 등으로 명시.
  3) 전역 작업규율(~/.claude/CLAUDE.md)은 모든 프로젝트에 자동 적용되므로,
     여기엔 "이 프로젝트만의" 규칙·함정·명령·좌표만 적는다. (공통 규율 복붙 금지)
═══════════════════════════════════════════════════════════════════ -->

# <프로젝트명> — 작업 가이드 (Claude 자동 로드)

> <한 줄 설명: 이 프로젝트가 무엇을 하는가 / 핵심 구조>
> **라이브 상태·다음 할 일은 항상 [HANDOFF.md](HANDOFF.md) 를 먼저 읽을 것.** <전체 기획이 있으면 [PRD.md](PRD.md)>

## 세션 규율 (핸드오프)
- **시작**: git 상태 확인(`git status`, `git log`, fetch) + `HANDOFF.md` 읽기 → 현재 상태·다음 할 일 보고 후, 사용자 선택받고 작업 시작. (추측 금지 — 실제 코드·git·실행결과가 1순위)
- **종료**: `HANDOFF.md` 최신화(지금 상태 / 방금 한 일 / 다음 할 일 / 함정 / 미검증) → 커밋·푸시는 **승인 후**.
- **검증 못 한 건 "미검증"으로 표시.** 실패·권한부족은 숨기지 말고 보고.

## 승인 필요 (매번 명시적으로) — 먼저 저지르지 말 것
- **커밋 / 푸시 / 배포 / DB 변경(insert·update·delete·마이그레이션) / 대량삭제 / 되돌리기 어려운 작업 / 외부 전송.**
- 비밀값(.env·키·토큰·비번·고객정보)은 **커밋·출력·외부전송 금지**.

## 이 프로젝트/환경의 함정 (반드시 인지)
- <예: 터미널 특이사항 / 빌드·실행 명령의 함정 / 배포 방식(자동? 수동?) / 마이그레이션 절차 / 비밀파일 위치 / 프레임워크 버전 주의 등>
- <이 프로젝트에서만 물리는 지뢰를 여기에. 없으면 "특이 함정 없음".>

## 명령어
```
<설치>
<개발 실행>
<빌드>
<테스트>
<배포>
```

## 운영 좌표
- <GitHub 레포 / 호스팅(Vercel 등) / DB / 주요 URL / 계정·팀명 등>
