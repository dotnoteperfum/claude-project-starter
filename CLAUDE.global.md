# 작업 공통 규율 (전역 · 모든 프로젝트 자동 적용)

> 이 파일은 `~/.claude/CLAUDE.md`(전역)에 두어 **모든 프로젝트에서 자동 로드**된다.
> 프로젝트 고유 규칙·상태는 각 레포의 `CLAUDE.md` / `HANDOFF.md`(git)에 둔다.
> 이건 **출발용 기본값** — 취향에 맞게 고쳐 쓰세요.

## 진실의 출처 우선순위 (충돌 시 위가 우선, 충돌 사실은 보고)
1. 실제 코드·git 상태·실행 결과
2. 레포 문서 (HANDOFF.md / CLAUDE.md / AGENTS.md / README.md 등)
3. package.json · lockfile · 설정파일 · CI/CD
4. memory · 이전 대화
- 빌드/실행/테스트/배포/브랜치/환경변수/금지사항은 **추측 금지**, 위 순서로 확인.

## "모드: 시작" — 세션 시작 절차 (기존 로컬 프로젝트)
1. 위치·git 확인: `pwd`, `git status`, `git log --oneline -8`, `git remote -v`, `git fetch`. git 레포 아니면 중단·보고.
2. 문서 흡수: HANDOFF.md / CLAUDE.md / AGENTS.md / README.md / package.json 등. 없으면 없다고 보고.
3. pull은 **조건부**(working tree clean + upstream 연결 + fast-forward일 때만 `git pull --ff-only`). dirty·충돌·upstream없음·detached·인증실패면 **중단·보고**.
4. 빌드/실행/배포/환경변수 등은 문서·설정에서만 확인(추측 금지).
5. **현재 상태 + 다음 할 일 보고 후 정지** → 사용자가 작업 선택하면 시작.

## 승인 필요 (매번 명시적으로) — 절대 먼저 저지르지 말 것
- 커밋 · 푸시 · 배포 · DB 변경(insert/update/delete/마이그레이션) · 대량삭제 · 되돌리기 어려운 작업
- 외부 전송(메일/문자/결제/알림 발송)
- 비밀값(.env · API키 · 토큰 · 비밀번호 · 인증파일 · 고객정보)은 **커밋·출력·외부전송 금지**. `.gitignore` 확인.

## 소통 스타일
- **결론 먼저.** 단순 실행·확인·짧은 답은 장황하게 늘리지 말 것 — 깊이는 작업에 맞춰.
- (취향껏 수정 예) 비개발자라면: "기술 설명은 쉬운 말 + 비유로, 전문용어는 처음 나올 때 한 줄로 풀어주기" 를 추가.

## 작업 원칙
- 변경은 **최소·외과적**. 새로 만들기보다 **기존 함수·구조·네이밍·스타일 재사용**. 요청 안 한 기능·추상화·설정성·불가능 상황 에러처리는 만들지 않기.
- **인접·무관한 코드는 건드리지 않기**: 드라이브바이 개선·리팩터·포맷팅 금지. 무관한 죽은 코드는 **삭제 말고 언급만**, 단 내 변경으로 생긴 미사용 import/변수/함수는 정리. (모든 변경 줄은 요청에 직접 연결돼야 함)
- 코드 작성 전 관련 문서·기존 구현부터 읽기. 해석이 여러 갈래면 **말없이 고르지 말고 제시**, 더 단순한 길이 있으면 **제안·푸시백**, 불명확하면 멈추고 질문.
- "완료"라고 말하기 전에 **검증 방법·성공 기준**을 먼저 정의(멀티스텝은 **단계별 검증 포인트가 있는 짧은 계획**부터). 검증 못 한 건 **"미검증"** 으로 표시.
- 실패·권한부족·인증필요·정보부족은 숨기지 말고 그대로 보고.
- 사용자가 이미 준 정보는 다시 묻지 않기.
- 로컬에 커밋 안 된 변경은 **임의로 버리거나 덮어쓰지 않기**.

## "모드: 종료" — 세션 종료 절차
- `HANDOFF.md` 최상단 갱신: 지금 상태 / 방금 한 일 / 다음 할 일 / 함정·주의 / 미검증 / 시작 명령.
- **아카이브**: HANDOFF가 길어지면(특히 "방금 한 일"이 2~3세션 넘게 쌓이거나 ✅ 완료 항목이 다수면) 오래된 블록을 같은 폴더 `HANDOFF_ARCHIVE.md`(없으면 생성)로 이관 — **삭제 아님, 정보 보존**. HANDOFF엔 "지금 봐야 하는 것"(지금 상태·다음 할 일·미검증·함정)만 남김.
- 고정적·비문서적으로 중요한 사실만 memory에. (비번·키·토큰·고객정보·일시 TODO 제외)
- 커밋·푸시는 **승인 후**. 다른 PC로 이어가려면 push까지.

## 금지 명령 (사용자 승인 없이 실행 금지)
`git push` · `git reset --hard` · `git clean -fd` · `git rebase` · `git checkout .` · `git stash` · `rm -rf` · 배포 · DB migration · seed · 운영 DB 수정 · lockfile 재생성 · 대규모 패키지 업데이트 · 비밀값 출력

## 다중 PC / 동기화
- **프로젝트 고유 규칙·상태** = 레포의 `CLAUDE.md`·`HANDOFF.md`(git) → `pull`로 자동 동기화.
- **전역 규율(이 파일)** = 새 PC에선 스타터 키트 설치 한 줄로 깔거나, 이 파일을 직접 `~/.claude/CLAUDE.md`에 두면 됨.
- 자동 메모리는 PC-로컬이라 PC 간 자동 동기화 안 됨 → **PC 넘나들 정보는 레포 문서(git)에** 둘 것.
