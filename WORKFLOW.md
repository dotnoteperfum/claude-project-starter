# Claude 작업 워크플로 가이드

> 새 프로젝트를 시작하거나, "인수인계가 왜 이렇게 도나" 헷갈릴 때 펴보는 문서.
> 비개발자 기준으로 쉽게 썼습니다.

---

## 1. 문서 4개의 역할 (뭐가 뭔지)

| 문서 | 무엇 | 언제 채워지나 | 어디에 |
|---|---|---|---|
| **PRD.md** | 뭘·왜·누구에게 만드나 (제품 기획) | **맨 먼저** (코드 전) | 각 프로젝트 레포 |
| **CLAUDE.md** | 이 프로젝트에서 *어떻게 일하나* (함정·명령어·좌표) | 스택·repo 생긴 뒤, 점점 | 각 프로젝트 레포 |
| **HANDOFF.md** | *지금 어디까지* 했나 (진행 상태) | 첫 세션부터 **매번** | 각 프로젝트 레포 |
| **전역 CLAUDE.md** (선택) | 모든 프로젝트 공통 작업 규율·취향 | 거의 안 바뀜 | `~/.claude/CLAUDE.md` (두면 모든 프로젝트에 자동 적용) |

> 자동 메모리(`~/.claude/.../memory`)도 있지만 **PC-로컬**이라 집↔회사로 안 넘어가고 잘 낡는다 → **믿지 말 것.** 중요한 건 항상 위 문서(git)에.

---

## 2. 인수인계(핸드오프)는 어떻게 작동하나 ★중요

**핵심 한 줄: 세션이 끝나면 "대화"는 사라진다. 다음으로 넘어가는 건 내가 HANDOFF.md에 적어서 push한 것뿐이다.**

PC를 건너가는 채널은 **git에 든 것 하나뿐**:

```
[작업/적용]  →  [HANDOFF.md에 기록]  →  [커밋·푸시]  →  [다음 세션이 pull]
```

이 사슬에서 **하나만 빠져도 인수인계가 끊긴다.** ("어제 했는데 또?" 의 원인은 거의 이 중 한 칸이 빠진 것.)

- ✅ **같은 PC** 다음 세션: 메모리는 자동으로 넘어감 (설정 불필요)
- ❌ **집↔회사** PC 간: 오직 **HANDOFF.md(git)** 로만. 메모리·대화는 안 넘어감.

**교훈: "적용했다"는 행위는 git에 흔적이 안 남는다.** (예: DB 마이그레이션 실행, 설정 변경) → 반드시 **HANDOFF에 글로 적고, 가능하면 출처에서 직접 검증**한 뒤 ✅ 표시.

---

## 3. 새 프로젝트 시작 순서

세 문서는 채워지는 **시점이 다르다**: PRD(먼저) → CLAUDE.md(스택 정한 뒤) → HANDOFF(매 세션).

```
PRD.md  ──→  CLAUDE.md  ──→  HANDOFF.md
(기획)       (스택 정한 뒤)    (매 세션)
```

### 4단계
1. **PRD부터.** 백지면 Claude에게 → *"이 아이디어로 PRD 잡아줘"* (← 진짜 첫 마디)
2. **스택 정하고 `git init`** (+ 필요한 서비스 생성) — ☞ 4번 항목 참고
3. *"이 프로젝트용 CLAUDE.md / HANDOFF.md 만들어줘"* → Claude가 템플릿 채워줌. 모르는 칸은 `<나중에 채움>` TODO로.
4. **작업 시작** → 모드:종료가 HANDOFF 갱신 / CLAUDE.md 함정은 부딪힐 때 추가.

### 기억할 것
- **1일차에 완성하려 하지 말 것.** 골격만 깔고 채워간다. 빈칸은 TODO로 표시됨.
- **HANDOFF는 코드 없어도 시작 가능** — §3 "다음 할 일"에 *세팅 체크리스트*를 적으면 그게 곧 진행 트래커.
- **첫 마디 = "PRD부터 같이 잡자"**, "CLAUDE.md 만들어줘"가 아니다.

---

## 4. "스택 정하기" — 비개발자용

> **스택 = 앱을 만드는 데 쓰는 도구·재료 묶음.** (가게 차릴 때 건물·주방기구·결제기·재료 고르는 것과 같음. 직접 만드는 게 아니라 **고르는** 것.)

### 정해야 하는 보통 4가지
| 질문 (일상어) | 기술 용어 | 예시 |
|---|---|---|
| 앱(화면)을 **뭘로 만들까?** | 프레임워크 | Next.js / React 등 |
| 데이터를 **어디에 저장?** (회원·주문 등) | 데이터베이스 | Supabase / Postgres 등 |
| 만든 걸 **어디 올려 손님이 보게?** | 호스팅·배포 | Vercel / Netlify 등 |
| 특별 기능이 **따로 필요한가?** | 부가 도구 | 결제·로그인·자동화 등 |

### 비개발자인 내가 정하는 법 (핵심: 외울 필요 없음)
```
나: PRD 요구를 말함 (예: "예약 받고, 외국인도 쓰고, 폰에서도 잘 보여야")
  ↓
Claude: "그럼 이 스택이 맞습니다 — A·B·C, 이유는 ○○" (추천 + 근거)
  ↓
나: "좋아, 그걸로" (승인)  ← 내 역할은 여기까지
```
- 고르는 기준은 **PRD의 요구**. 기획이 서 있으면 스택은 거의 자동으로 정해진다.
- 모르면 *"이거랑 저거 뭐가 달라? 비개발자한테 추천은?"* → 장단점 + 추천 하나 받기.
- 비용·난이도 걱정되면 미리 말하기(예: "무료로 시작하고 싶어") → 추천이 거기 맞춰 바뀜.

---

## 5. HANDOFF 관리 — 다이어트 & 아카이브

- HANDOFF가 길어지면 신호 대비 잡음이 커진다 → **오래된 이력은 `HANDOFF_ARCHIVE.md`로 이관**(삭제 아님, 정보 보존).
- HANDOFF엔 **"지금 봐야 하는 것"**만: 지금 상태 / 다음 할 일 / 미검증 / 함정.
- 이 아카이브 규칙은 `HANDOFF.md` 템플릿 맨 위에 적혀 있어, "모드:종료" 때 Claude가 보고 알아서 정리한다. (전역 규율 파일을 따로 두면 모든 프로젝트에 자동 적용도 가능)

---

## 6. 여러 PC / 다른 곳에서 쓰기

- **프로젝트 문서**(PRD·CLAUDE·HANDOFF): 프로젝트를 git 원격(GitHub 등)에 올려두면 `pull`로 어느 PC서든 이어집니다.
- **이 키트**(템플릿·new-project): 다른 PC에서도 쓰려면 그 PC에서 설치 한 줄을 다시 실행:
  ```powershell
  irm https://raw.githubusercontent.com/dotnoteperfum/claude-project-starter/main/install.ps1 | iex
  ```
- **로컬 비밀파일**(.env, 인증 세션 등): git에 올리지 말 것 → 각 PC에서 따로 둡니다.

---

## 한 장 요약
1. **새 프로젝트 첫 마디 = "PRD부터 잡자."** (CLAUDE.md/HANDOFF는 그 다음)
2. **스택 = 도구 4개 고르기**, 고르는 건 Claude 추천 + 나는 승인.
3. **인수인계는 HANDOFF.md(git)로만 PC를 건넌다.** 적용했으면 *글로 적고 push*까지.
4. **문서는 자라는 것** — 1일차 완성 강박 버리기. 빈칸은 TODO.
